define( function () {

/**
 * @name AppState
 * @constructor 
 * @param attrs.module {Module}     the module currently being viewed
 * @param attrs.example {Example}   the example curently being viewed
 */
    appState = new Backbone.Model();

    return appState;
});
